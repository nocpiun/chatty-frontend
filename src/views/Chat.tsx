import React, { useEffect } from "react";

// import Socket from "@/socket";

const Chat: React.FC = () => {
    useEffect(() => {
        // const socket = Socket.get();

        // socket.connect("/chat?r=", {
        //     onOpen() {
                
        //     },
        //     onMessage(packet) {
                
        //     },
        //     onError(err) {
        //         throw err;
        //     }
        // });

        // return () => socket.closeCurrentConnection();
    });

    return (
        <div></div>
    );
};

export default Chat;
